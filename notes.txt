sumo-gui -c 7065twin/7065.sumocfg      # loads with the real-world view (7065.view.xml)
# or, to inspect/edit lanes, connections, TLS:
netedit 7065twin/7065.net.xml

netedit -s 7065_slip.net.xml -g 7065_aerial.view.xml


All 8 charts for 0320 are there and discovered correctly. To run it:


cd atspm_digitizer

# single new day (add --plot for QA overlays)
uv run atspm-digitizer tmc --date 0320 --data-dir ../tmc/7065 --plot

# or re-run everything (0320 + all previously added days) with a fresh consolidated.csv
uv run atspm-digitizer tmc --all --data-dir ../tmc/7065 --plot
Since you likely want 0320 folded into one combined dataset with the other days (0507/0508/0511), use --all — it reprocesses every day found in tmc/7065/ and rewrites tmc/result/7065/consolidated.csv. Note this run is slower than before (~a few minutes for 4 days) since the OCR validation now checks both the full image and the header band for each chart.

After it finishes, regenerate the plots too:


uv run python scripts/visualize_results.py --result-dir ../tmc/result --out-dir ../tmc/testing
Want me to run it now?